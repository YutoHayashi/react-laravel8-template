import React from 'react';
import { Layout } from './Layout';
export const GetStarted: React.FC<{}> = ( {  } ) => {
    return (
        <Layout>
            <h2 className='font-bold text-xl'>初めに</h2>
        </Layout>
    );
};
