import React from 'react';
import { Admin } from '@/components/layouts/Admin';
export const Top: React.VFC<{}> = ( {  } ) => {
    return (
        <Admin>
            <p>document top.</p>
        </Admin>
    );
};
