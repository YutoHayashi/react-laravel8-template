import React from 'react';
import { Main } from '@/components/layouts/Main';
export const NotFound: React.FC<{}> = ( {  } ) => {
    return (
        <Main>
            not found
        </Main>
    );
};
