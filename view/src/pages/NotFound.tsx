import React from 'react';
import { Main } from '@/components/layouts/Main';
export const NotFound: React.VFC<{}> = ( {  } ) => {
    return (
        <Main>
            not found
        </Main>
    );
};
